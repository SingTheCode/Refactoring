// 예시
let _title = "untitled";

result += `<h1>${title()}</h1>`;

setTitle(obj["articleTitle"]);

function title() {
  return _title;
}

function setTitle(arg) {
  _title = arg;
}

// 상수 이름 바꾸기
const companyName = "애크미 구스베리";
